recipes:
  hackernews:
    description: "Hacker News front page and search recipes."
    source: "./recipes/hackernews"
    trusted: true

  deepl:
    description: "DeepL translation recipe."
    source: "./recipes/deepl"
    trusted: true

  x:
    description: "X/Twitter recipe (requires bird CLI and auth env vars)."
    source: "./recipes/x"
    trusted: true

  brave-search:
    description: "Brave Search recipe."
    source: "./recipes/brave-search"
    trusted: true

  wikipedia:
    description: "Wikipedia search and read recipe."
    source: "./recipes/wikipedia"
    trusted: true

  allenai:
    description: "Allen AI Playground recipe (OLMo, Tülu, Molmo models)."
    source: "./recipes/allenai"
    trusted: true

  web-reader:
    description: "Fetch and extract readable text from any URL."
    source: "./recipes/web-reader"
    trusted: true

  openstreetmap:
    description: "Geocoding, reverse geocoding, routing, and place search via OpenStreetMap."
    source: "./recipes/openstreetmap"
    trusted: true
