[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "-m 'not e2e'"
testpaths = ["tests"]
markers = [
  "e2e: live-network checks against external recipe providers",
]
